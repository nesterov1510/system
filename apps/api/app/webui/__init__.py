"""Python server-rendered web UI (Jinja2 + HTMX) for MSB.

Этот пакет заменяет прежний React/Next.js фронтенд: страницы рендерит сервер
на Python (Jinja2-шаблоны), динамика (доска, вкладки, модалки) — через HTMX,
чат/уведомления — через нативный WebSocket. Node/npm/сборка не нужны.

Бизнес-логика НЕ дублируется: роуты этого пакета переиспользуют те же
модели, сервисы, матрицу прав (`app.core.permissions`) и функции API-роутеров.
"""
