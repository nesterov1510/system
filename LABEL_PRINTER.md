# Принтер этикеток `3B-350B` — raw TSPL, 192.168.8.75:9100

Принтер этикеток **не подключён к CUPS**. Он слушает raw-сокет
`192.168.8.75:9100` и читает язык **TSPL**, поэтому PDF ему отправить нельзя —
MSB собирает этикетку как монохромный растр 203 dpi (8 точек на мм) и шлёт его
напрямую командой `BITMAP`. Кириллица и туркменские буквы при этом не зависят
от прошивки принтера: в растр они попадают уже из DejaVu Sans, а QR рисуется
тем же qrcode, что и в PDF-версии.

```text
MSB API ── рендер растра 58×38 ──▶ print-agent ── TCP 192.168.8.75:9100 ──▶ 3B-350B (TSPL)
```

Бланки A4 при этом печатаются **отдельно** — в локальную очередь CUPS
`office_printer_a4` (режим `cups_local`); их эта инструкция не трогает.

| Что печатаем | Куда | Режим в MSB |
|---|---|---|
| Бланки приёмки A4 | очередь CUPS `office_printer_a4` | `cups_local` |
| Этикетки 58×38 мм | `192.168.8.75:9100` (TSPL) | `raw_tspl` |

> **Порт 9100 — это raw-порт принтера** (JetDirect/AppSocket), а не порт CUPS
> (631). В режиме `raw_tspl` MSB говорит с принтером напрямую по этому порту и
> шлёт TSPL, поэтому CUPS вообще не участвует. Не путайте с режимами
> `cups_remote`/`ipp`, где 631 — порт CUPS/IPP.

При приёмке печатаются **две** этикетки 58×38:

1. **Этикетка на технику** — номер ремонта, имя и телефон заказчика, отмеченные
   комплектация и внешние дефекты, QR на закрытую карточку `/repairs/{id}`,
   подпись `MERYOSAB electronics`.
2. **Этикетка клиенту** — номер ремонта, крупный QR на публичную страницу
   `/r/{token}` (без логина) со статусом, условиями хранения и юридической
   информацией; строка про срок хранения (`storage_months`) прямо на этикетке.

Повторная печать любой из двух — из карточки ремонта («🏷 Этикетка» /
«🏷 Клиенту (QR)») или из меню ⚡ в списке «Все ремонты».

---

## 1. Проверка принтера

С машины, где работает print-agent:

```bash
nc -zv 192.168.8.75 9100        # порт должен открыться
```

Проверить, что принтер принимает TSPL, можно, послав готовое задание из
`printed/` (агент всегда сохраняет копию) или простую команду:

```bash
printf 'SIZE 58 mm, 38 mm\r\nCLS\r\nTEXT 10,10,"4",0,1,1,"MSB test"\r\nPRINT 1\r\n' \
  | timeout 5 nc 192.168.8.75 9100
```

Если принтер не отвечает, сначала проверьте сеть/фаервол — настройки MSB не
починят недоступный принтер.

---

## 2. Настройка в интерфейсе MSB

Откройте:

```text
Админ → Настройки → Принтер → Принтер этикеток 58×38 мм
```

| Поле | Значение |
|---|---|
| Режим печати | **Напрямую по сети (TSPL, порт 9100)** |
| Адрес принтера | `192.168.8.75` |
| Порт | `9100` |
| Имя очереди CUPS | пусто (не используется в raw-режиме) |
| Media option | не используется в raw-режиме |

Нажмите **«Сохранить этикетки»**, затем **«Тестовая этикетка»** и проверьте
статус в блоке «Последние задания».

Адрес и порт по умолчанию берутся из env API `MSB_LABEL_HOST` / `MSB_LABEL_PORT`
(по умолчанию `192.168.8.75` / `9100`); сохранённое в админке значение имеет
приоритет.

### Параметры TSPL (env, необязательно)

```ini
MSB_LABEL_SPEED=4       # скорость печати
MSB_LABEL_DENSITY=8     # плотность (0-15)
MSB_LABEL_GAP_MM=2      # зазор между этикетками
# MSB_LABEL_DIRECTION=1 # раскомментировать, если этикетка выходит зеркальной
```

---

## 3. Как устроено задание

print-agent получает из очереди payload с `raw_base64` (растр TSPL) и шлёт его
в сокет. Задание выглядит так (байты растра идут сразу после запятой в `BITMAP`):

```text
SIZE 58 mm, 38 mm
GAP 2 mm, 0 mm
SPEED 4
DENSITY 8
CLS
BITMAP 0,0,58,304,0,<17632 байта растра>
PRINT 1,1
```

Полярность растра TSPL: `1` = белое (точка не печатается), `0` = чёрное.
Ширина строки — 58 байт (464 точки / 8), высота 304 точки.

---

## 4. Диагностика

### Задание осталось в очереди

```bash
sudo systemctl status msb-print-agent
sudo journalctl -u msb-print-agent -n 150 --no-pager
```

### `connection refused` / таймаут

Проверьте доступность порта и маршрут:

```bash
nc -zv 192.168.8.75 9100
ping -c 3 192.168.8.75
```

### Этикетка пустая / зеркальная / обрезана

1. пустая — принтер не понял растр: убедитесь, что режим в админке `raw_tspl`,
   а не CUPS, и что порт 9100;
2. зеркальная — задайте `MSB_LABEL_DIRECTION=1` и перезапустите API;
3. обрезана — проверьте физический размер этикетки 58×38 и `MSB_LABEL_GAP_MM`.

### QR не сканируется

Распечатайте сохранённую копию из `printed/` и отсканируйте с экрана; если на
экране читается, а с бумаги нет — увеличьте `MSB_LABEL_DENSITY`.
